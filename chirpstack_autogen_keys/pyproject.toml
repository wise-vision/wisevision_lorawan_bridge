[project]
name = "chirpstack-autogen-keys"
version = "0.1.0"
description = "Automatic ChirpStack api key and app id generator"
authors = [{ name = "Cezary", email = "office@wisevision.tech" }]
requires-python = ">=3.10"

dependencies = [
    "chirpstack-api",
    "grpcio",
    "protobuf",
    "pyyaml",
    "docker"
]

[tool.uv]
